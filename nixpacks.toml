# Fuerza despliegue Python/Flask en Coolify (no sitio estático ni Node)
[variables]
NIXPACKS_PYTHON_VERSION = "3.12"
FLASK_APP = "run:app"

[start]
cmd = "flask db upgrade && gunicorn --bind 0.0.0.0:${PORT:-3000} run:app"
